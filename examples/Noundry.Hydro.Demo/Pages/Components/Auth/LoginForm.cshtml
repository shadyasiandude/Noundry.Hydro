@model LoginForm

<div class="min-h-screen flex items-center justify-center bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full space-y-8">
        <div class="text-center">
            <h2 class="mt-6 text-3xl font-extrabold text-gray-900">
                Sign in to your account
            </h2>
            <p class="mt-2 text-sm text-gray-600">
                Welcome to the Noundry.Hydro Demo
            </p>
        </div>

        <form class="mt-8 space-y-6" on:submit="@(() => Model.SignIn())">
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <noundry-alert type="error" dismissible="true" title="Login Failed">
                    @Model.ErrorMessage
                </noundry-alert>
            }

            <div class="space-y-4">
                <!-- Using Noundry.UI form components -->
                <noundry-text-input 
                    label="Email Address" 
                    name="@nameof(Model.Email)"
                    type="email"
                    placeholder="Enter your email address"
                    required="true"
                    bind="@Model.Email"
                    autocomplete="email" />

                <noundry-text-input 
                    label="Password" 
                    name="@nameof(Model.Password)"
                    type="password"
                    placeholder="Enter your password"
                    required="true"
                    bind="@Model.Password"
                    autocomplete="current-password" />

                <!-- Using Noundry.UI checkbox component -->
                <noundry-checkbox 
                    label="Remember me" 
                    name="@nameof(Model.RememberMe)"
                    bind="@Model.RememberMe" />
            </div>

            <div class="space-y-4">
                <!-- Using Noundry.UI button component -->
                <noundry-button 
                    type="submit"
                    variant="primary" 
                    size="lg"
                    full-width="true"
                    loading="@Model.IsLoading"
                    loading-text="Signing in..."
                    disabled="@Model.IsLoading">
                    Sign in
                </noundry-button>

                <div class="text-center">
                    <noundry-button 
                        type="button"
                        variant="ghost" 
                        size="sm"
                        on:click="@(() => Model.ClearForm())">
                        Clear Form
                    </noundry-button>
                </div>
            </div>

            <!-- Using Noundry.TagHelpers conditional rendering -->
            <div asp-if="@Model.Options.DevelopmentMode" class="border-t border-gray-200 pt-4">
                <p class="text-center text-sm text-gray-600 mb-3">Demo Credentials (Development Mode)</p>
                <div class="grid grid-cols-3 gap-2">
                    <noundry-button 
                        type="button"
                        variant="secondary" 
                        size="xs"
                        on:click="@(() => Model.FillDemoCredentials("admin"))">
                        👑 Admin User
                    </noundry-button>
                    
                    <noundry-button 
                        type="button"
                        variant="secondary" 
                        size="xs"
                        on:click="@(() => Model.FillDemoCredentials("manager"))">
                        👔 Manager User
                    </noundry-button>
                    
                    <noundry-button 
                        type="button"
                        variant="secondary" 
                        size="xs"
                        on:click="@(() => Model.FillDemoCredentials("customer"))">
                        👤 Customer User
                    </noundry-button>
                </div>
            </div>

            <div class="text-center">
                <p class="text-sm text-gray-600">
                    Don't have an account?
                    <a href="/Identity/Account/Register" class="font-medium text-indigo-600 hover:text-indigo-500">
                        Sign up here
                    </a>
                </p>
            </div>
        </form>
    </div>
</div>